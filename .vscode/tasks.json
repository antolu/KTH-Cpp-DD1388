{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "knapsack",
            "type": "shell",
            "command": "make clean && make knapsack",
            "options": {"cwd": "${workspaceFolder}/07-knapsack", },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}