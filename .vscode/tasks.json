{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "knapsack",
            "type": "shell",
            "command": "make clean && make knapsack",
            "options": {"cwd": "${workspaceFolder}/07-knapsack" },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
<<<<<<< HEAD
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        },
        {
            "type": "shell",
            "label": "cpp build active file",
            "command": "/usr/bin/cpp",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        },
        {
            "type": "shell",
            "label": "chess",
            "command": "make",
            "args": [
                "chess"
            ],
            "options": {
                "cwd": "${workspaceFolder}/06-chess"
            },
            "group": "build"
            "label": "meta"
        },
        {
        "type": "shell",
            "command": "make meta",
            "options": {"cwd": "${workspaceFolder}/09-meta" },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}